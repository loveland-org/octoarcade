# Default triage automation

name: Triage new issues
description: Automatically triage new issues using default skills

on:
  issues:
    types: [opened]

skills:
  - detect-duplicates
  - fill-out-issue-metadata
  - detect-low-quality-issues
  - assign-to-copilot